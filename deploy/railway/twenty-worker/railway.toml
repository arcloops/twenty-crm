[build]
builder = "NIXPACKS"
buildCommand = "yarn install --immutable && yarn nx build twenty-server"

[deploy]
startCommand = "node packages/twenty-server/dist/queue-worker/queue-worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
